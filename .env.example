PRIVATE_KEY=0xyour_evm_private_key
# PRIVATE_KEY=0xyour_other_evm_private_key

# Which Salt deployment to run against: testnet (default) or mainnet.
# ⚠ mainnet uses REAL FUNDS — Send/Swap move actual assets. See the README.
#   npm run dev:testnet   (or just `npm run dev`)
#   npm run dev:mainnet
SALT_ENV=testnet

# Only needed for the "Policy chat" menu item (the natural-language policy agent):
ANTHROPIC_API_KEY=
